{% extends 'base.html.twig' %}

{% block title %}TypeOfCompany{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center mb-3">Type : {{ type_of_company.type }}</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_company.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_of_company.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_company_index') }}" class="btn btn-outline-dark">back to list</a>

    <a href="{{ path('app_type_of_company_edit', {'id': type_of_company.id}) }}" class="btn btn-success ">edit</a>

    {{ include('type_of_company/_delete_form.html.twig') }}
</div>
{% endblock %}
