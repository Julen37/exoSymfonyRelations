<form method="post" action="{{ path('app_type_of_company_delete', {'id': type_of_company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_of_company.id) }}">
    <button class="btn btn-danger">Delete the type</button>
</form>
